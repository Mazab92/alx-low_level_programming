#include <stdio.h>
/**
 * main - a program to alphabet in lowercase, followed by a new line
 * Return: 0
 */
int main(void)
{
	char C;

	C = 'a';
	while
		(C <= 'z') {
			putchar(C);
			C++;
		}
	putchar('\n');
	return (0);
}
