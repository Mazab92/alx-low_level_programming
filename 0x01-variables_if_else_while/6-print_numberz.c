#include <time.h>
#include <stdio.h>
#include <stdlib.h>

/**
 * main - a program that prints the lowercase alphabet in reverse
 * Return:: 0
 */
int main(void)

{
	int z;

	for (z = '0'; z <= '9'; z++)
	putchar(z);
	putchar('\n');
	return (0);
}
